
git             # распределённая система управления версиями файлов
clone           # создает связанную с целевым репозиторием копию
fork            # (вилка) создает независимую копию
branch          # (ветка) параллельная версия репозитория
commit          # (фиксировать) фиксация изменений
pull            # (вытащить) получение последних изменений из репозитория
pull request    # запрос на слияние форка с основным репозиторием
push            # (выдвинуть) отправка коммитов в репозиторий
merge           #  (слить) слияние веток
upstream        # (вверх по течению)
fetch           # извлечение информ. о последних изменениях
merge commit    # (коммит слияния) связывает две ветки
HEAD            # указатель на коммит, который станет родителем следующего коммита
hash            # уникальная идентификация объектов (коммиты, файлы, тэги, деревья)
tag             # человекопонятный способ пометить уникальный коммит
# (Обычные тэги имеет смысл использовать для приложения к коммиту какой-либо информации)
lightweight tags# (легковесные тэги) только из имени и ссылки на коммит
tree-ish        # (похожий на дерево) это то, что указывает на коммит (можно передавать как аргумент команды)

# TODO: разобрать
cherry-pick
interactive rebase
fixup commits
squash
изменение remote