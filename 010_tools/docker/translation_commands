Использование: docker [ПАРАМЕТРЫ] КОМАНДА

Самодостаточная среда выполнения для контейнеров

Опции:
      --config string   расположение файлов конфигурации клиента (по умолчанию "/home/san/.docker")
  -c, --context string  имя контекста, используемого для подключения к демону (переопределяет DOCKER_HOST env var
                           и контекст по умолчанию, установленный с помощью "docker context use")
  -D, --debug       Включить режим отладки
  -H, --host list   сокет(ы) демона для подключения к
-l, --log-level string    Задает уровень ведения журнала ("debug"|"info"|"warn"|"error"|"fatal") (по умолчанию "info")
      --tls      Использует TLS; подразумевается --tlsverify
      --tlscacert string   сертификаты доверия, подписанные только этим центром сертификации (по умолчанию "/home/san/.docker/ca.pem")
      --tlskey string      путь к файлу сертификата TLS (по умолчанию "/home/san/.docker/cert.pem")
      --tlsverify          путь к файлу ключа TLS (по умолчанию "/home/san/.docker/key.pem")
      --tlsverify     Используйте TLS и проверьте удаленный
  -v, --version       Распечатайте информацию о версии и завершите работу

Команды управления:
    builder     Управление сборками
    config      Управление конфигурациями Docker
    container   Управление контейнерами
    context     Управление контекстами
    image       Управление изображениями
    manifest    Управление манифестами изображений Docker и списками манифестов
    network     Управление сетями
    node        Управление узлами роя
    plugin      Управление плагинами
    secret      управление службой Docker secrets
    service     Управление службами
    stack       управление стеком Стеками Docker
    swarm       управление роем Управление
    system      системой роя Управление доверием Docker
    trust       Управление доверием к изображениям Docker
    volume      Управление томами

Команды:
    attach      Прикрепление локальных стандартных потоков ввода, вывода и ошибок к запущенному контейнеру
    build       Создание образа из файла Dockerfile
    commit      Создает новое изображение на основе изменений контейнера
    cp          Копирование файлов /папок между контейнером и локальной файловой
    create      Создание нового контейнера
    diff        Проверка изменений файлов или каталогов в файловой системе контейнера
    events      Получайте события в реальном времени с сервера
    exec        Выполнить команду в запущенном контейнере
    export      Экспорт файловой системы контейнера в виде истории архива tar
    history     Отображение истории
    images      Список изображений изображения
    import      Импортируйте содержимое из архива для создания образа файловой системы
    info        Отображение общесистемной информации
    inspect     Возврата низкоуровневой информации об объектах Docker
    kill        Убить один или несколько запущенных контейнеров
    load        Загрузите изображение из архива tar или STDIN
    login       Вход в реестр Docker
    logout      Выход из журналов реестра Docker
    logs        Извлечение журналов контейнера
    pause       Приостановить все процессы в одном или нескольких контейнерах
    port        портов сопоставления портов или конкретное сопоставление для контейнера
    ps          Список контейнеров
    pull        Извлекать изображение или репозиторий из реестра
    push        Переместить изображение или репозиторий в реестр
    rename      Переименовать контейнер
    restart     Перезапустить один или несколько контейнеров
    rm          Удалить один или несколько контейнеров
    rmi         Удалить одно или несколько изображений
    run         Выполнить команду в новом контейнере
    save        Сохранить одно или несколько изображений в архив tar (по умолчанию передается в STDOUT)
    search      Поиск изображений в Docker Hub
    start       Запуск статистики одного или нескольких остановленных контейнеров
    stats       статистики использования ресурсов контейнера (контейнеров) в реальном времени
    stop        Остановка одного или нескольких запущенных контейнеров
    tag         Создайте тег TARGET_IMAGE, который ссылается на SOURCE_IMAGE
    top         Отображаются запущенные процессы контейнера
    unpause     приостановку Отменить приостановку всех процессов в одном или нескольких контейнерах
    update      Обновление конфигурации версии одного или нескольких контейнеров
    version     информации о версии Docker
    wait        ожидания до тех пор, пока один или несколько контейнеров не остановятся, затем распечатайте их коды выхода

Запустите "docker COMMAND --help" для получения дополнительной информации о команде.